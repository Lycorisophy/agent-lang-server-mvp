spring:
  application:
    name: agent-lang-server-mvp
  # 未指定 spring.profiles.active 时使用 local，以加载 application-local.yaml（本机 MySQL）
  profiles:
    default: local
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
